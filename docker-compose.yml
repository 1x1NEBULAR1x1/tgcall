services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
      network: host
    ports:
      - "8080:8080"
    env_file:
      - ./backend/.env
    restart: unless-stopped
